# Uncomment the next line to define a global platform for your project
platform :ios, '11.0'
use_frameworks!

target 'Beacon SDK iOS' do

  	pod 'KontaktSDK', '~> 2.0'
    pod 'Alamofire', '~> 4.3'

end
